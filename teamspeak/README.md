# Teamspeak

To run docker-compose up you have to adapt `.env`

## Usage

Set the following environment variables:

```env
TS3SERVER_DB_PASS=
DOMAIN=
```

And then run

```sh
docker-compose up -d
```
